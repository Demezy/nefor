// Lexical composition only: parent traversal and absolute-looking segments are
// deliberately preserved rather than confined or canonicalized.
let join: fn(String, String) -> String = |base, segment| => if (=)(base, "") then segment else if (=)(segment, "") then base else str(str(base, "/"), segment)
