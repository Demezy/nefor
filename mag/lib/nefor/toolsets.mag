// MAG bindings for the config-owned neutral toolset manifest. The compiler's
// read-json parser validates JSON and converts its values into MAG data.
let manifest = read_json("nefor/toolsets.json")

let read_only = get(manifest, "read_only")

let general = get(manifest, "general")
